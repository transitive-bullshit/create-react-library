{
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "license": "{{license}}",
  "author": "{{author}}",
  "repository": "{{repo}}",
  "main": "build/index.js",
  "module": "build/index.es.js",
  "engines": {
    "node": ">=8",
    "npm": ">=5"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "crl-scripts build",
    "start": "crl-scripts start",
    "test": "crl-scripts test",
    "deploy": "crl-scripts deploy",
    "eject": "crl-scripts eject",
    "prepare": "crl-scripts build"
  },
  "peerDependencies": {
    "react": ">=16"
  },
  "dependencies": {
    "prop-types": "^15.7.2"
  },
  "devDependencies": {
    {{#if typescript}}"@types/jest": "^24.0.11",{{/if}}
    {{#if typescript}}"@types/react": "^16.8.13",{{/if}}
    {{#if typescript}}"@types/react-dom": "^16.8.4",{{/if}}
    "crl-scripts": "^0.0.2",
    "react": "^16.8.6"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
